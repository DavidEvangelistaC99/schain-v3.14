[build-system]

requires=[
    "meson-python",
    "numpy>2.3",
    "cython"
]

build-backend="mesonpy"

[project]

name = "schain"
version = "1.0.0"
requires-python = ">=3.14"
dependencies = [
    "numpy>=2.3",
    "scipy>=1.16"
]
